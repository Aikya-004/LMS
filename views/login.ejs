<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <title><%= title %></title>
</head>
<body>
<%- include('header', { title: 'Login' }) %>

<div class="grid grid-cols-6 md:grid-cols-6 gap-4">
    <div class="col-start-2 col-span-4 md:col-span-6 bg-sky-600 p-6 rounded-lg shadow-md mx-auto my-24">
        <h1 class="text-4xl font-bold text-white mb-8">Login</h1>
        <form action="/login" method="POST">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <div class="mb-4">
                <label for="email" class="block text-white text-sm font-semibold mb-2">Email</label>
                <input type="email" id="email" name="email" class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300" required>
            </div>
            <div class="mb-4">
                <label for="password" class="block text-white text-sm font-semibold mb-2">Password</label>
                <input type="password" id="password" name="password" class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300" required>
            </div>
            <button type="submit" class="bg-white text-white-900 px-4 py-2 rounded-md hover:bg-sky-300 focus:outline-none focus:ring focus:border-white-700 text-white-900">Login</button>
        </form>
        <p class="text-white mt-4">Don't have an account? <a href="/signup" class="text-black-300">Sign up now</a></p>
    </div>
</div>

<%- include('footer') %>
</body>
</html>

